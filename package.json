{
  "name": "campuspots",
  "private": true,
  "scripts": {
    "web": "pnpm --filter @campuspots/web",
    "contracts": "pnpm --filter @campuspots/contracts",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}
